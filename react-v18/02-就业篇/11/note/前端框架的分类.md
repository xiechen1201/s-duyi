# 前端框架的分类

前端框架除了 react 和 vue 还有 angular 等，还出现了 svelte、solid js 等，这些框架有什么区别？

## 自变量和因变量

现代前端框架有一个非常重要的特点，那就是基于状态的声明式渲染。、

UI = f(state)

state 的变化会导致最终计算出来的 UI 发生变化，UI 在这里就是因变量。

## 框架的分类

state 实际上就是自变量，自变量直接或者间接改变了 UI。